"use strict";
class Mobile {
    //like here we are defing these in the class which implements this interface
    constructor(cameraMode, pixels) {
        this.cameraMode = cameraMode;
        this.pixels = pixels;
    }
    sharma() {
        console.log("hello");
    }
}
